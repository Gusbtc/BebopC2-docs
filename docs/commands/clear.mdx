---
title: clear
description: Clear current operator terminal.
---

# clear

## Usage

```text
[BEBOP ~]$ clear
```

## How it works

Operator-only command. The browser terminal calls the xterm clear path locally; no teamserver route and no beacon task are used.

## Output

No output. The visible terminal buffer is cleared.
